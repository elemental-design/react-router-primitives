const {
  Route,
  Switch,
  generatePath,
  matchPath,
  withRouter,
} = require('react-router');

const { BrowserRouter: Router } = require('react-router-dom');

module.exports = {
  Router,
  Route,
  Link,
  Switch,
  matchPath,
  generatePath,
  withRouter,
};
